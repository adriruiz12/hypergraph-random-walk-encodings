[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "eompp"
version = "0.1.0"
description = "Random-walk-induced message passing on hypergraphs - shared experiment core"
requires-python = ">=3.9"
dependencies = ["numpy", "scipy", "torch"]

[tool.setuptools]
packages = ["eompp"]
